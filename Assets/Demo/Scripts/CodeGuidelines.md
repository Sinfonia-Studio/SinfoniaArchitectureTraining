## 設定

- cs, md, txtのファイルはUTF-8を使用する
- AIによるファイル変更の使用はサマリー追加、コメント追加、タイポ修正の編集に留める

## 順序

1. **コンストラクタ**
2. **イベント (public events)**
3. **パブリックプロパティ**
4. **インターフェースプロパティ**
5. **パブリック定数(const/readonly)**
6. **パブリックメソッド**
7. **パブリックインターフェースメソッド**
8. **パブリックEnum定義**
9. **パブリックclass定義**
10. **パブリックStruct定義**
11. **定数 (const/readonly)**
12. **インスペクター表示フィールド ([SerializeField])**
13. **プライベートフィールド**
14. **Unityライフサイクルメソッド (Awake, Start, Updateなど)**
15. **イベントハンドラメソッド (プレフィックスがHandleのタイプ)**
16. **プロテクトインターフェース・仮想メソッド**
17. **プライベートメソッド**
18. **内部メソッド、抽出メソッド**
19. **プライベートEnum定義**
20. **プライベートclass定義**
21. **プライベートStruct定義**
22. **デバッグ機能**

## サマリー・コメント

- 全てのメソッドにはサマリーを付ける
- パブリックなプロパティとイベントにはサマリーを付ける
- プロパティ, イベント, フィールド変数かつ一行のサマリーは以下のように一行かつスペースを入れて書く
/// <summary> comment </summary>
- クラス, メソッドまたは二行以上のサマリー以下のように改行してインデントを上げて書く
/// <summary>
///     comment1
///     comment2
/// </summary>
- コメントは基本的に日本語を使用する
- 日本語の最後は「。」で終える
- メソッド内の処理には適宜コメントを付ける
- SerializeField属性を使用するフィールドには、TooltipAttributeでの説明を付ける

## 命名規則

- C#のフィールド変数はキャメルケースでプレフィックスに `_`
- C#のクラス、プロパティ、メソッドはパスカルケース
- C#の引数はキャメルケース
- 定数はアッパースネークケース
- イベントのプレフィックスは `On`
- イベントハンドラメソッドのサフィックスは `Handler`
- インターフェースは `I` で始める
- メソッドは動詞で始める
- bool型のプロパティは `Is` または `Has` で始める
- uxml, ussの命名はロウワーチェーンケース

## その他

- `using` ディレクティブはファイルの先頭にまとめて記述する
- フィールド変数を公開する際はプロパティを使用する
- アクセス修飾子は必ず明示的に記述する
- マジックナンバーは排除し、代わりに定数で定義する
- 一行のブロックの場合でも以下のように、波カッコを使用する
{ Console.WriteLine(”message”); }

## UnityAPI

- データコンテナとして使用するScriptableObject（CreateAssetMenuが付く物）は
外部からのアクセスにはパラメータを読み取り専用にして
SerializeFieldのみで変更できるようにする